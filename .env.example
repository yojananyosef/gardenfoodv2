NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Shared secret for external cron triggers (optional)
CRON_SECRET=
# Absolute public site URL (used for metadataBase, sitemap, robots, OG)
NEXT_PUBLIC_SITE_URL=https://gardenfoodv2.vercel.app
# Mercado Pago (único proveedor). Credenciales TEST en sandbox, APP_USR en prod.
MP_ACCESS_TOKEN=
# Secret para validar webhooks MP (x-signature). Ver panel MP > tu app > Webhooks.
MP_WEBHOOK_SECRET=
# Opcional: email coleccionador para guard `collector==payer` en TEST (ej. TEST collector email)
MP_COLLECTOR_EMAIL=